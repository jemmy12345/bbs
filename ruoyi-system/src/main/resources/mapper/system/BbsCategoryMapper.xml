<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.BbsCategoryMapper">
    
    <resultMap type="BbsCategory" id="BbsCategoryResult">
        <result property="categoryId"    column="category_id"    />
        <result property="categoryName"  column="category_name"  />
        <result property="categoryDesc"  column="category_desc"  />
        <result property="icon"          column="icon"          />
        <result property="sortOrder"     column="sort_order"     />
        <result property="status"        column="status"        />
        <result property="postCount"     column="post_count"     />
        <result property="createBy"     column="create_by"     />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"      column="update_by"      />
        <result property="updateTime"    column="update_time"    />
        <result property="remark"        column="remark"        />
    </resultMap>
    
    <sql id="selectBbsCategoryVo">
        select category_id, category_name, category_desc, icon, sort_order, status, post_count, create_by, create_time, update_by, update_time, remark 
        from bbs_category
    </sql>
    
    <select id="selectBbsCategoryById" parameterType="Long" resultMap="BbsCategoryResult">
        <include refid="selectBbsCategoryVo"/>
        where category_id = #{categoryId}
    </select>
    
    <select id="selectBbsCategoryList" parameterType="BbsCategory" resultMap="BbsCategoryResult">
        <include refid="selectBbsCategoryVo"/>
        <where>
            <if test="categoryName != null and categoryName != ''">
                AND category_name like concat('%', #{categoryName}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        order by sort_order, create_time desc
    </select>
    
    <insert id="insertBbsCategory" parameterType="BbsCategory" useGeneratedKeys="true" keyProperty="categoryId">
        insert into bbs_category
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="categoryName != null and categoryName != ''">category_name,</if>
            <if test="categoryDesc != null">category_desc,</if>
            <if test="icon != null">icon,</if>
            <if test="sortOrder != null">sort_order,</if>
            <if test="status != null">status,</if>
            <if test="postCount != null">post_count,</if>
            <if test="remark != null">remark,</if>
            <if test="createBy != null and createBy != ''">create_by,</if>
            create_time
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="categoryName != null and categoryName != ''">#{categoryName},</if>
            <if test="categoryDesc != null">#{categoryDesc},</if>
            <if test="icon != null">#{icon},</if>
            <if test="sortOrder != null">#{sortOrder},</if>
            <if test="status != null">#{status},</if>
            <if test="postCount != null">#{postCount},</if>
            <if test="remark != null">#{remark},</if>
            <if test="createBy != null and createBy != ''">#{createBy},</if>
            sysdate()
        </trim>
    </insert>
     
    <update id="updateBbsCategory" parameterType="BbsCategory">
        update bbs_category
        <set>
            <if test="categoryName != null and categoryName != ''">category_name = #{categoryName},</if>
            <if test="categoryDesc != null">category_desc = #{categoryDesc},</if>
            <if test="icon != null">icon = #{icon},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="status != null">status = #{status},</if>
            <if test="postCount != null">post_count = #{postCount},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            update_time = sysdate()
        </set>
        where category_id = #{categoryId}
    </update>
    
    <delete id="deleteBbsCategoryById" parameterType="Long">
        delete from bbs_category where category_id = #{categoryId}
    </delete>
    
    <delete id="deleteBbsCategoryByIds" parameterType="Long">
        delete from bbs_category where category_id in 
        <foreach item="categoryId" collection="array" open="(" separator="," close=")">
            #{categoryId}
        </foreach>
    </delete>

    <update id="incrementPostCount" parameterType="Long">
        update bbs_category set post_count = post_count + 1 where category_id = #{categoryId}
    </update>

    <update id="decrementPostCount" parameterType="Long">
        update bbs_category set post_count = post_count - 1 where category_id = #{categoryId}
    </update>

    <select id="selectHotCategoryList" resultMap="BbsCategoryResult">
        <include refid="selectBbsCategoryVo"/>
        where status = '0'
        order by post_count desc, sort_order
    </select>
    
</mapper>
