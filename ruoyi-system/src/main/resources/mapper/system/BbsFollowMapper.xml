<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.BbsFollowMapper">
    
    <select id="selectBbsFollow" resultType="int">
        select count(*) from bbs_follow 
        where user_id = #{userId} and follow_user_id = #{followUserId}
    </select>
    
    <insert id="insertBbsFollow">
        insert into bbs_follow (user_id, follow_user_id, create_time)
        values (#{userId}, #{followUserId}, sysdate())
    </insert>
    
    <delete id="deleteBbsFollow">
        delete from bbs_follow 
        where user_id = #{userId} and follow_user_id = #{followUserId}
    </delete>

    <select id="countFollow" parameterType="Long" resultType="int">
        select count(*) from bbs_follow where user_id = #{userId}
    </select>

    <select id="countFans" parameterType="Long" resultType="int">
        select count(*) from bbs_follow where follow_user_id = #{userId}
    </select>
    
</mapper>
