<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.BbsTagMapper">
    
    <resultMap type="BbsTag" id="BbsTagResult">
        <result property="tagId"         column="tag_id"         />
        <result property="tagName"       column="tag_name"       />
        <result property="tagColor"      column="tag_color"      />
        <result property="useCount"      column="use_count"      />
        <result property="createTime"    column="create_time"     />
    </resultMap>
    
    <select id="selectBbsTagById" parameterType="Long" resultMap="BbsTagResult">
        select tag_id, tag_name, tag_color, use_count, create_time
        from bbs_tag
        where tag_id = #{tagId}
    </select>
    
    <select id="selectBbsTagByName" parameterType="String" resultMap="BbsTagResult">
        select tag_id, tag_name, tag_color, use_count, create_time
        from bbs_tag
        where tag_name = #{tagName}
    </select>
    
    <select id="selectBbsTagList" parameterType="BbsTag" resultMap="BbsTagResult">
        select tag_id, tag_name, tag_color, use_count, create_time
        from bbs_tag
        <where>
            <if test="tagName != null and tagName != ''">
                AND tag_name like concat('%', #{tagName}, '%')
            </if>
        </where>
        order by use_count desc, create_time desc
    </select>
    
    <select id="selectTagsByPostId" parameterType="Long" resultMap="BbsTagResult">
        select t.tag_id, t.tag_name, t.tag_color, t.use_count, t.create_time
        from bbs_tag t
        inner join bbs_post_tag pt on t.tag_id = pt.tag_id
        where pt.post_id = #{postId}
    </select>
    
    <insert id="insertBbsTag" parameterType="BbsTag" useGeneratedKeys="true" keyProperty="tagId">
        insert into bbs_tag (tag_name, tag_color, use_count, create_time)
        values (#{tagName}, #{tagColor}, #{useCount}, sysdate())
    </insert>
     
    <update id="updateBbsTag" parameterType="BbsTag">
        update bbs_tag
        <set>
            <if test="tagName != null and tagName != ''">tag_name = #{tagName},</if>
            <if test="tagColor != null">tag_color = #{tagColor},</if>
            <if test="useCount != null">use_count = #{useCount},</if>
        </set>
        where tag_id = #{tagId}
    </update>
    
    <delete id="deleteBbsTagById" parameterType="Long">
        delete from bbs_tag where tag_id = #{tagId}
    </delete>
    
    <delete id="deleteBbsTagByIds" parameterType="Long">
        delete from bbs_tag where tag_id in 
        <foreach item="tagId" collection="array" open="(" separator="," close=")">
            #{tagId}
        </foreach>
    </delete>

    <update id="incrementUseCount" parameterType="Long">
        update bbs_tag set use_count = use_count + 1 where tag_id = #{tagId}
    </update>

    <update id="decrementUseCount" parameterType="Long">
        update bbs_tag set use_count = use_count - 1 where tag_id = #{tagId}
    </update>
    
</mapper>
